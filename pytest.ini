[pytest]
testpaths = tests
python_files = test_*.py
addopts = --verbose
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    benchmark: marks tests for benchmarking
    g1_mjx: compiles the Unitree G1 MJX step (~20 GB RSS); skipped on small hosts
